const express = require('express');

const router = express.Router();




router.post("/", (req, res) => {
 


})

router.get("/", (req, res) => {

    


}) //get all products

router.get("/:id")

router.put("/:id")

router.patch("/:id")

router.delete("/:id")

router.delete("/")













module.exports = router;